[pytest]
testpaths = tests
addopts = -q
# Put the repo root on sys.path so `pytest tests/` works the same as
# `python -m pytest` (imports like `tools`, `evals`, `tests.conftest`).
pythonpath = .
# Keep collection out of generated app code under workspace/
norecursedirs = workspace .venv
